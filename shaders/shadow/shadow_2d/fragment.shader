// Depth-only: nothing to write. Depth is captured automatically into the FBO's
// GL_DEPTH_ATTACHMENT. Kept as an explicit empty main() because the
// Core::Shader loader requires both a vertex and a fragment file.
void main() {
}
