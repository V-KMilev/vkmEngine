// Skinning is a vertex-stage difference and nothing else, so the shading is the
// static program's own file rather than a copy of it. One place to edit, and a
// lobe added there can never reach only half the scene.
#include "../pbr/fragment.shader"
